name: deploy

on:
    push:
        branches:
            - main

jobs:
    build:
        name: build
        runs-on: ubuntu-latest

        steps:
            - name: checkout repo
              uses: actions/checkout@v3

            - name: setup node
              uses: actions/setup-node@v3

            - 